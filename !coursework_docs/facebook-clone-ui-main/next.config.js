/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains:["icons.iconarchive.com", "platform-lookaside.fbsbx.com", "images.pexels.com", "www.flaticon.com"],
  },
};


module.exports = nextConfig;
