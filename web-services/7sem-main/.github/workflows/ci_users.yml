# .github/workflows/ci.yml

on:
  pull_request:
  push:
    branches: [Non-existent_branch._To_make_it_work_replace_it_with_main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test
        run: |
          cd WS/lab6_cicd/usersService
          npm install
          npm run test

      - name: Deploy
        # Only run this step if the branch is main
        if: github.ref == 'refs/heads/main'
        env:
          deploy_url: ${{ secrets.RENDER_DEPLOY_HOOK_URL_USERS }}
        run: |
          curl "$deploy_url"